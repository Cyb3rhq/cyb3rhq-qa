- hosts: localhost
  tasks:
    - docker_container:
        name: cyb3rhq-master
        state: absent
        force_kill: true
    - docker_container:
        name: cyb3rhq-worker1
        state: absent
        force_kill: true
    - docker_container:
        name: cyb3rhq-worker2
        state: absent
        force_kill: true
    - docker_container:
        name: cyb3rhq-worker3
        state: absent
        force_kill: true
    - docker_container:
        name: cyb3rhq-agent1
        state: absent
        force_kill: true
    - docker_container:
        name: cyb3rhq-agent2
        state: absent
        force_kill: true
