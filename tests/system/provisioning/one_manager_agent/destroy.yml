- hosts: localhost
  tasks:
  - docker_container:
      name: cyb3rhq-manager
      state: absent
      force_kill: yes
  - docker_container:
      name: cyb3rhq-agent1
      state: absent
      force_kill: yes
