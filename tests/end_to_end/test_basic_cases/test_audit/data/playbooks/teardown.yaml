- name: Cleanup manager environment
  hosts: manager
  tasks:

    - name: Delete cyb3rhq audit rules file
      become: true
      file:
        path: /etc/audit/rules.d/cyb3rhq.rules
        state: absent

    - name: Delete audit rules
      become: true
      shell: auditctl -D
